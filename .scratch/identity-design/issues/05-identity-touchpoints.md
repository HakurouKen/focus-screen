# 确定 Sidelit 在系统与文档中的呈现规范

Type: grilling
Label: wayfinder:grilling
Status: resolved
Assignee: codex
Parent: ../map.md
Blocked by: 02, 03, 04

## Question

将确定的名称、图标与文案放到各触点中审视：Finder 应用包名称、系统授权列表、登录项、菜单栏辅助功能名称、退出菜单和 README，分别应如何呈现？明确用户可见名称与技术标识的边界，并形成可供实施的验收清单。依赖最终文案与图标决定，本票不实施改名或制作正式资产。

## Comments

### 推荐的最终呈现规范（等待用户确认）

| 触点 | 目标 |
| --- | --- |
| Finder 应用包 | Sidelit.app |
| 应用名称元数据 | Sidelit |
| 系统辅助功能列表、登录项 | 新安装与注册后显示 Sidelit；实际系统显示在实施时检查 |
| 应用图标 | 已确定的 A 并排双屏图标 |
| 菜单栏图标辅助功能名称 | Sidelit |
| 菜单栏 tooltip | 已确定的当前运行状态文案 |
| 退出菜单 | 退出 Sidelit |
| 登录项说明 | 请在系统设置的登录项中允许 Sidelit。待允许状态下，再次点击开关可取消注册。 |
| README 标题与介绍 | Sidelit；专注当前屏幕，调暗其他屏幕。 |
| 构建和运行示例 | dist/Sidelit.app；固定安装位置示例 /Applications/Sidelit.app |
| 应用内可执行文件 | Sidelit，与应用包及元数据保持一致 |

身份与历史边界：

- 保留 bundle identifier `local.leroy.focus-screen` 与现有 UserDefaults 配置键。品牌更名不另建应用身份或主动重置用户设置。
- 仓库目录名 focus-screen 及历史研究、提交、票据中的旧名称无需追溯改写；自动化测试的隔离 suite 名不是用户界面，不需改名。
- 保留当前菜单栏应用定位，不为展示新图标增加 Dock 常驻或新窗口。
- 旧构建产物不由脚本擅自删除。实施说明应区分旧 FocusScreen.app 与新 Sidelit.app，避免运行旧包导致混淆。
- 当前是 ad-hoc 签名构建；本规划不承诺已有辅助功能授权或登录注册自动迁移。以实际安装路径验证，必要时说明重新授权或重新注册步骤。

实施验收清单：

1. 构建得到 Sidelit.app，包内可执行文件与元数据一致，正式图标资源正确打包。
2. 运行界面、辅助功能名称、登录项提示、README 的用户操作示例使用 Sidelit 和已确认文案。
3. 菜单栏单色图标在系统深浅外观下清晰，状态切换不改变图形；应用图标在 Finder 的常用尺寸下清晰。
4. 使用原有设置验证启用状态与调暗程度的读取，不重置配置；不修改已有调暗行为。
5. 0%、100%、暂停、单屏、未授权与无法确定当前屏幕的文字与行为吻合；说明文档不把恢复原样描述为主动调亮。
6. 在固定位置安装后检查系统授权列表与登录项名称；真实登录注册测试由实施阶段按用户授权范围执行，记录未经实测的部分。
7. 完成项目已有自动测试和构建；复核用户可见文件中的旧品牌残留，区分有意保留的技术标识与历史记录。

以上规范待用户确认后即可关闭地图并交付实施；没有开始改名或正式资产制作。

## Answer

用户确认上方呈现规范及实施验收清单。各用户可见触点统一使用 Sidelit，新应用包为 Sidelit.app；正式图标与文案分别以已关闭的图标票、文案票为准。

保留现有应用标识与设置键，不因品牌更名主动重置配置。系统授权与登录项名称需在实施时验证，不承诺现有授权或注册自动迁移。

本地图所需设计决定已全部完成，可交付实施。应用改名、正式图标制作、构建与安装验证尚未执行，属于后续实施工作。
