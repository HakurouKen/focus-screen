专注当前屏幕，调暗其他屏幕。

- 适用于 macOS 13+，Universal 包同时支持 Apple Silicon 和 Intel。
- 下载 ZIP，解压后将 Sidelit.app 放入「应用程序」，首次运行时授予辅助功能权限。
- 此版本使用 ad-hoc 签名，未经过 Apple 公证；首次打开可能需要在「系统设置 → 隐私与安全性」中允许。更新后如辅助功能权限失效，请移除旧授权并重新添加。
- 同目录的 .sha256 文件用于核对下载完整性，可运行 `shasum -a 256 -c Sidelit-<版本>-universal.zip.sha256`。
